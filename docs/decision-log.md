# Decision log

One line per decision, newest last. Format: date, decision, why, trade-off noted.

| # | Date | Decision | Why | Trade-off noted |
| --- | --- | --- | --- | --- |
| D1 | 2026-09-10 | The site is a standalone Next.js 15 project in `site/` beside the prototype, with its own git repository and Vercel project, on port 3200 locally. No code is imported from the product. | Brief, scope and section 7. The Grownz reference sits on 3100, so the two can run side by side. | None. |
| D2 | 2026-09-10 | Tokens are measured from the approved prototype into `src/styles/tokens.json` and generated into `tokens.css` with a SHA-256 test. Dark is the default theme on `:root`; light is the backup on `data-theme="light"`. | Brief, Design system and rule 4; Grownz D22 for the dark default. | When the client uploads a fuller design system, `tokens.json` is replaced and the generator re-run; the site-level display scale stays in `globals.css`. |
| D3 | 2026-09-10 | The prototype's muted grey `#696969` fails WCAG AA on the ground (3.4:1), so the site's muted text token is `#9A968F` (6.6:1 on the ground, 5.4:1 on cards). The prototype value is kept only for decorative rules. Coral small text on cards uses `#F07A7A` (5.6:1). | Brief rule 6 and the QA protocol: contrast measured on the dark theme. | The designer's grey is one step lighter than drawn. |
| D4 | 2026-09-10 | Featured cards (Pro, Signature, Founders Circle Bundle) use a 1 px crimson-to-coral gradient ring with the glow shadow, not the prototype's solid crimson fill. | Brief, Design system: the gradient is used as light, never as a large fill; section 5 names the ring on the featured pricing card. | The prototype's filled cards read louder; the ring plus glow keeps the emphasis with less ink. Reversible in one class. |
| D5 | 2026-09-10 | Every phone frame on the site shows a real capture, or one of the three screens cropped from the prototype's hero composite with a "Design preview" badge, or a "capture pending" state. Nothing is mocked. | Brief rule 2 and Grownz D23. The prototype's phone renders are design mockups. | Until the owner signs in for captures, several frames show previews or pending states; the pages are built so captures drop in with no code change. |
| D6 | 2026-09-10 | People cut-outs, 3D objects and FAQ scenes are resolved at build time from `public/assets` and render nothing (or a token-drawn glyph for objects) until the image plan is approved and produced. | Brief section 8 requires a priced image plan and approval before Magnific credits are spent; the balance check itself was refused by the session's permission classifier. | The checkpoint 3 home page ships without people; the layout is ready for them. |
| D7 | 2026-09-10 | Start free and Sign in both land on the app's unified sign-in screen, which is also where an account is created (email or mobile number, then a code). "Get your card" lands on the store at prm.linkist.ai/store. | The product signs in and registers on one screen; there is no separate registration URL to point at. | If a dedicated registration route appears in the audit, `START_URL` changes in one place. |
| D8 | 2026-09-10 | The store prices cards in AED with an approximate US dollar figure; the prototype's round USD card prices are marketing approximations. The site shows the prototype's USD and AED figures with the currency switch and says AED is being confirmed, until the audit settles which currency bills. | Store walk-through on 10 September: Starter PVC AED 75 (about $20), Signature metal AED 225 (about $61). | Confirm list C5. The bundle arithmetic in the prototype only holds with a metal Signature card; the savings tables now say so and derive every figure from the price data. |
| D9 | 2026-09-10 | The three unsourced statistics on linkist.ai (88 percent of cards discarded, 60 percent forget context, 70 percent of leads die) are not used anywhere on the site. | Brief rule 1: statistics only with a cited primary source. | None. |
| D10 | 2026-09-10 | GDPR and SOC 2 do not appear on the site. PDPL is referenced only as "the privacy policy is written under the UAE Personal Data Protection Law (Federal Decree-Law No. 45 of 2021)", which the published policy states. | Brief rule 3: compliance words only with evidence. The prototype's comparison row "GDPR and SOC 2 Type 2 data security" is dropped from the table. | The FAQ answer on AI and data is rewritten to what the policy says: encryption in transit and at rest. |
| D11 | 2026-09-10 | Motion is on by default with a footer switch remembered on the device and a `?motion=off` override; switching off stops Lenis at once. Hero motion is CSS keyed on a pre-paint script; scroll reveals load GSAP lazily. | Brief section 6, Grownz D24, D30 and D31. | The OS reduced-motion preference is not read, as the brief instructs; the switch is one click. |
| D12 | 2026-09-10 | The mini mockups replay Linkist flows with example figures (a contact record filling in, a natural-language search, ICP chips lighting up, a nudge dropping in, a day list ticking off, two contact cards sliding apart, a card tapping a phone) on a 9 s loop, paused off-screen, frozen at the end state when motion is off, every one labelled "example". | Brief section 5 and 6; Grownz D26. | They are not app screens and never sit inside a device frame. |
| D13 | 2026-09-10 | Checkpoint 3 also builds the pages the home page links to (how it works, features hub and five feature pages, use cases hub and five pages, NFC cards, bundles, pricing, teams), so no link on the home page dead-ends. Learn, help, chat, legal, about, customers, contact, changelog, AI and security wait for the audit and the client's answers. | A home page whose navigation 404s cannot be reviewed as a home page. The AI and security pages depend on the confirm list. | Those pages are built from the same content data and will change where the audit changes a fact. |
| D14 | 2026-09-10 | The site's own legal links point at the company's published documents on linkist.ai (privacy, terms) until the legal hub ships in checkpoint 6, and support links use support@linkist.ai, which both public pages and the terms publish. | Never a printed placeholder address; never a dead link. | None. |
| D15 | 2026-09-10 | The header and footer carry two product buttons: **Get the App**, landing on the PRM app's unified sign-in and registration screen at prm.linkist.ai/UnifiedAuth, and **Get NFC Card**, landing on the card product's sign-in at m.linkist.ai/login, as the owner directed. Sign in stays as a text link on desktop and in the menu; Start free elsewhere lands on the same unified screen. The desktop navigation now starts at 1280 px to make room; the menu button serves below that. | Card enquiries are a large share of demand, so the card product gets a first-class button. Both URLs are constants in `src/lib/site.ts` with environment overrides. | The morning walk-through had read m.linkist.ai as the PRM app; the owner's instruction and the published articles (profile addresses of the form m.linkist.ai/me/name) place the card product there. Corrected as R8. |
| D16 | 2026-09-10 | The display scale is raised to the sizes measured on linkist.ai's home page: 72 px headlines and 56 px section titles at 1440, 36 px and 32 px at 390. The hero grid becomes 5:3 so the text column is 715 px at 1440, and below that the headline follows its column with container units. | Owner request. Measured in the page with the site's DM Sans: the widest hero line, "Act at the right time.", is 9.54 px per pixel of font size, so 72 px needs 686 px. | Article titles use the section size (56 px), not the headline size, because they run to 90 characters. |
| D17 | 2026-09-10 | The hero is a two-slide carousel, the PRM app and the NFC cards, with tabs, arrows, swipe, arrow keys, an 8 s auto-advance that pauses on hover and focus and stops once a visitor takes control; the inactive slide is inert. The intent chips move into "How Linkist works", where the three stage screens sit in a row joined by a rail that fills towards the next stage (3.6 s a step, stopping on a click); a chip lights the stage its moment belongs to. The use cases sit in a three-plus-two grid. | Owner request for the hero, the chips, the stage visual and the card arrangement. | The stacked deck and the tilted fan are deleted, not kept behind a flag. Motion off freezes the carousel, the rail and the flow at once. |
| D18 | 2026-09-10 | The blog: the seven articles published on linkist.ai/blogs are imported verbatim (`pnpm import:blog`, from the live pages) into `content/blog` and served at their existing addresses, /blogs and /blogs/[slug], in the old page's structure rebuilt with the site's tokens: featured article, category filters, cards, newsletter; category tag, title, author row, cover, chapters, body, FAQ JSON-LD, related reading. /learn redirects to /blogs. The authors' text keeps its punctuation, including em dashes, so the imported bodies are outside rule 5 and the QA scan; the old closing CTA blocks are dropped for the site's band; links to old pages map to the new ones; the same Unsplash pictures are downloaded under the Unsplash licence. | Owner request: pull the blogs, keep the design. Keeping the URLs protects the search standing the old pages have earned. | If the owner wants the em dashes normalised (C15), one pass over `content/blog` does it. |
| D19 | 2026-09-10 | The AI and security pages are written from the published privacy policy and terms of service (version 1.1, 1 June 2026) with the section cited on every card, plus what the product shows, and each carries a "not published yet" section. They make no claim either document does not make. | Checkpoint 4 lists both pages; C8 and C9 are unanswered, but the documents answer most of what a sceptical reader asks. | Provider names, hosting region, the AI credit table and any attestation stay listed as gaps until the client answers. |
| D20 | 2026-09-10 | The ten launch articles the brief planned for `/learn` are written as blog posts at `/blogs`, bylined "Linkist", dated the day they were written, with title-card covers rendered from the tokens (`pnpm covers`) rather than photographs. Each traces every product claim to the truth sheet and carries 3 FAQ pairs. | D18 put the blog at the old site's address; one place for articles is simpler than two. No image budget is approved (C4), and a title card is honest about being a title card. | When the image plan runs, photographs can replace the title cards file by file. |
| D21 | 2026-09-10 | The help centre grows from 49 to 109 entries, written from the product's public screens and store, the privacy policy and terms (cited where an answer rests on them), the prototype's plan comparison and the owner's instructions; answers about the signed-in app say so until the audit confirms them. The same corpus powers the full-page assistant and the widget. | Brief 4 asks for 80 to 120 real Q&As and a Limits category that states section 3.9 plainly. | Entries that depend on C2 are phrased as pending, not omitted. |
| D22 | 2026-09-10 | The changelog lists only dated events with a public source (the website, the blog, the legal documents), then what the product holds today without a published launch date, then what the product says is coming. No product release is given a date it does not have. | Rule 1. Linkist has not published release notes (C17). | When the team publishes notes, they join the timeline under a Product tag. |
| D23 | 2026-09-10 | The community page promises only what the sign-up delivers: product notes, new articles and invitations when there is an event, with no fixed calendar. Its three channels are the ones the current linkist.ai footer links to. | Rule 1; the prototype's sign-up copy says "community" and nothing more specific. | Any programme the team runs later is added when it exists. |
| D24 | 2026-09-10 | The legal hub reproduces the company's published Terms of Service and Privacy Policy verbatim (`pnpm import:legal`, version 1.1, 1 June 2026), marked "in force" with a link to the original and the note that the published version prevails. The other eight documents are drafts for counsel, dated, marked "not in force", with every open point as a visible `[PLACEHOLDER: ...]` marker, excluded from the sitemap and set to noindex. | Brief 4 asks for ten dated documents with placeholder markers for counsel; rule 1 forbids presenting a draft as a policy in force. The footer now links the internal copies (replacing D14's external links). | When counsel signs a draft off, its status flips to published in the front matter and it joins the sitemap. |
| D25 | 2026-09-10 | The contact page lists the published addresses (support@, privacy@, dpo@), the phone and WhatsApp number the current site publishes, and the company address; partnerships, press and security routes say "no dedicated address yet" until the environment provides one, and the form carries the topic. | Rule 1: no invented address. C12 remains open. | A published security address replaces the support fallback in one variable. |
| D26 | 2026-09-10 | The about page names the company, the address, the controller, the law and the timeline from public sources, and states that the team is named when confirmed (C10). The customers page names no customer, shows what the site can show, presents three example networks labelled as examples, and invites design partners without promising what they receive. | Brief 3.9 and 4: honest proof, no testimonials without consent. | The design-partner terms are a `[PLACEHOLDER]` in spirit: the page says the company is settling them. |
| D27 | 2026-09-11 | The sitemap carries real last-modified dates for articles and legal documents and the build date elsewhere, with priorities by page role and legal drafts excluded; `NEXT_PUBLIC_NOINDEX=true` turns every page to noindex and robots.txt to disallow-all, for previews and for any deployment whose host is not yet the real domain. | Brief 7 (sitemap, robots, noindex on internal routes) and the C1 situation: a preview must not compete with linkist.ai for the same pages. | Unset the variable on the launch deployment or nothing is indexed. |
| D28 | 2026-09-11 | Meta descriptions are clamped at a sentence boundary inside 160 characters by `pageMeta`; long feature and use-case page titles are shortened to under 70 characters; imported article titles keep their published length. An SEO audit script (`pnpm seo`) checks every route for status, unique sized titles and descriptions, canonical, robots, Open Graph and Twitter tags with a 1200 x 630 image that resolves, one H1, JSON-LD that parses, unresolved markers, em dashes outside the imported articles, every internal link and image, and the 404 probe. | Brief 10's SEO items, made repeatable. The first run found one false positive (internal paths that redirect to the product) and 50 length warnings, now 0 problems. | Titles of the seven imported articles stay long; changing them changes the articles. |
| D29 | 2026-09-11 | Every button, chip, tab, form control and navigation link is at least 44 px tall; small buttons, nav links, the wordmark, breadcrumbs, contents links, footer text buttons and the carousel arrows were raised to meet it. Text links inside running text and in spaced lists (footer columns, related reads) keep their natural size under the WCAG 2.2 target-size exemptions, which the layout scan applies: inline links in prose and heading anchors are exempt, and an undersized link passes when a 24 px circle centred on it touches no other target. | Brief 10 asks for 44 px targets; a footer column with 44 px rows would double in height for no gain, and WCAG 2.2 AA itself allows the spacing exemption. | The layout scan reports every remaining undersized control as a warning so nothing slips in silently. |
| D30 | 2026-09-11 | The QA protocol of brief 10 is tooling, not a checklist read once: `pnpm review` at eight widths, `pnpm a11y`, `pnpm seo`, `pnpm api` (forms and API behaviour incl. 400 on bad bodies, 429 with retry-after, 503 when unconfigured, the security headers and the redirects), `pnpm layout` (44 px targets, launcher overlap, tablet newsletter width), `pnpm lighthouse` (every route, mobile and desktop, threshold 95), `pnpm browsers` (Chromium, Firefox, WebKit, an iPhone and a Pixel emulation, with the assistant exercised on each) and `pnpm spell` (cspell, British English, a project dictionary). An independent reviewer reads the site against Appendix B and writes docs/08-independent-review.md. | Nothing ships with an open red, and the same runs repeat at every later release. | Lighthouse and the browser pass take time; they run after the other checks, when the machine is quiet. |
| D31 | 2026-09-11 | The independent review's findings are fixed in one pass: the footer keeps its launcher clearance at every width and its first column a minimum width with the newsletter stacked; the page's H1 is a permanent screen-reader heading with both hero slide headlines as H2s; the motion switch drops the ?motion=off override when switched on; the cookie-choices button appears only when a notice exists and the notice links the internal privacy page; the assistant panel follows the launcher in the tab order, takes focus on open and returns it on close, and on phones the launcher waits for the first scroll; the pricing FAQ tells one story on currency; /privacy, /terms, /choose-plan and /digital-business-card redirect to their new pages and the trust pages link the internal legal copies; the hero headline enters with movement only so it counts as painted; the wordmark is not preloaded; the chat route answers 200 without a key; the share row copies the page the visitor is on; article rows say Published; the 404 page has its own title; and the copy fixes (16 countries, NFC phones, digits for counts, glossary spellings, no promised reply time, no repeated line under the hero). | The review found two high, eight medium and sixteen low items; every one that the code could fix is fixed, the rest are confirm-list items. | The DNS inconsistency for m.linkist.ai (C21) and the Team plan's yearly unit (C5) are the client's to settle. |
| D32 | 2026-09-11 | Lighthouse runs from Playwright's Chromium on a debugging port (the launcher package cannot spawn here), on every route in both form factors with simulated throttling, and its table is committed as docs/08-lighthouse.md. Two changes came out of it: the hero headline enters with movement only so the largest paint is not delayed by a fade, and the assistant panel and its help corpus load on demand rather than in every page's first load. Desktop scores 100 throughout; on mobile 34 of 48 routes reach 95 and the rest sit at 87 to 94 because the simulator, fed by a local HTTP/1.1 server where every script lands before first paint, treats all 18 script files as needed for the paint. The HTTP/2 preview is the measurement of record. | Brief 10 asks for 95 on every route; the honest reading of the local number is recorded rather than tuned around. | No further code change is made for the simulator's sake; the observed largest paint is under 360 ms on every page. |
| D33 | 2026-09-11 | The site deploys to its own Vercel project, `linkist-marketing` on the Linkist account (linkistai@gmail.com, scope `vivek-chandrans-projects-e6f77bb4`), which the owner created on 10 September and connected to the GitHub repository, so a push to `main` is a production release (the CLI equivalent is `vercel deploy --prod --yes` in that scope), with `NEXT_PUBLIC_SITE_URL`, `NEXT_PUBLIC_APP_URL`, `NEXT_PUBLIC_CARD_APP_URL` and `NEXT_PUBLIC_NOINDEX=true` set in every environment and Node pinned to 22.x in `package.json` so the deployed runtime matches the tested one. The asset URL for Open Graph images derives from Vercel's own `VERCEL_PROJECT_PRODUCTION_URL` until the domain exists. | The brief named the team `bettroi-website`, which the owner corrected on 11 September as a mistake from another project: this site belongs to Linkist only; the project created in that team that morning, and one created on another account after an accidental browser login, were both deleted with nothing deployed. The domain is not decided (C1), so every page carries noindex and robots disallows everything until launch (D27). | Launch is three Vercel edits and a redeploy: the domain, `NEXT_PUBLIC_SITE_URL`, and removing `NEXT_PUBLIC_NOINDEX`; documented step by step in docs/09-handover.md. |
| D34 | 2026-09-11 | A GitHub Actions workflow (`.github/workflows/qa.yml`) runs tokens, types, lint, unit tests, spelling and a production build on every push and pull request, and on demand runs `pnpm browsers` (Chromium, Firefox, WebKit, an iPhone and a Pixel) against a deployed site. | The Firefox pass could not run on the build machine (checkpoint 8); a Linux runner has no such limit, and the checks job catches a broken build before Vercel does. | The Firefox result is read from the workflow's run and its screenshot artefact rather than from this machine; the browsers job is started from the Actions tab or `gh workflow run qa.yml -f base=<url>`. |
| D35 | 2026-09-11 | The NFC cards on the site are rendered faces made with Nano Banana 2 through Magnific from the real brand mark (uploaded as the reference), one per material plus the Founders Circle finish, cut out and delivered as WebP; the name and company stay HTML, so no text is generated. The home hero's card slide puts the three cards on a slow 3D ring (18 s cycle): the ring turns a third at a time, and while it rests the front card comes forward and flips to show a token-drawn back. Motion off leaves the ring still. | The owner asked on 11 September for realistic cards carrying the Linkist icon in the hero, the cards section and the bundles, and for the circling and flipping hero; the brief's rule that logos are composited rather than generated is kept in spirit by generating from the mark itself and checking the result against it. 387 credits. | Product photographs from the client (C3) replace the four files with no code change; the ring and flip are CSS only (typed custom properties), no script. |
| D36 | 2026-09-11 | Three visual changes at the owner's request: every card and floating chip is glass (a translucent gradient over an 18 px blurred backdrop with a lit top edge, the surface token underneath as the fallback and the light theme unchanged); the alternate section grounds are deeper black gradients rather than flat charcoal; and the brand reds move one step more vibrant, crimson `#CE394D` to `#D63A57`, deep crimson `#B1394B` to `#B93049`, coral `#E85F5F` to `#F2606B`, coral ink `#F07A7A` to `#F5828C`, with the glow following. | Owner, 11 September: glassmorphism on the cards, blacker gradient sections like the pricing band, reds more vibrant without tipping into alarm red. Contrast re-measured: white on the new crimson 4.56:1, coral on cards 4.8:1, coral ink on cards 6.1:1, all AA (D3 stands). | `tokens.json` changed, the generated CSS and its checksum test updated; the prototype's exact values are recorded here for anyone comparing. Blur costs a little paint time on long pages; the Lighthouse run of the next release tells. |
| D37 | 2026-09-11 | The card faces are regenerated without a logo and the real mark is composited in HTML at one size (11 percent of the card width, top left) on every card, front and back; each material also has a rendered back (plain, the NFC wave centred, an engraved line), used by the hero flip and by the NFC cards page deck. The hero ring is more cinematic: a tilted camera that breathes, a 140 px ring radius, and the front card swinging 180 px towards the visitor with a nod before it turns. The NFC cards page hero is a deck: the front card lifts, turns over and slides to the rear every 6 s, and the deck tilts towards the pointer. | Owner, 11 September: the generated marks differed in size; the back was the old CSS; the pop should come closer and move more fluidly; the cards page wanted the same idea in a different style. Compositing the mark also restores the brief's rule that logos are never generated. | 624 more credits (8 renders, 8 removals; 1,011 in total). Both effects are CSS keyframes on typed custom properties; the pointer tilt is a 12-line client component. |
| D38 | 2026-09-11 | The NFC cards page hero is a plain revolve: the three cards turn on a ring once every 24 s, fronts and backs passing, nothing lifts, flips or pops; the pointer tilt stays. The home hero's ring is layered above its floating chips so the card that swings forward is never covered. Sign in leaves the header and the menu; Get the App is the way into the product. | Owner, 11 September: the deck's peeking and turning read as wrong; the popped hero card was being cut across by a chip; Sign in is redundant beside Get the App, which lands on the same unified screen (D7). | The footer keeps no Sign in either; a returning user uses Get the App. |
| D39 | 2026-09-11 | Glass, second pass: every card carries a 1 px gradient ring that is bright at its top-left and bottom-right corners and a soft light source in the top-left, over the blurred glass; featured cards keep their crimson conic ring. The alternate section grounds are black with one soft charcoal form and a faint crimson breath, so the glass has something to show through. Light theme: flat cards, no rings. | Owner, 11 September, with a glassmorphism reference: shinier corners, and a minimal form behind the cards, in black rather than the reference's blue. Three grounds were previewed side by side (black with a soft form, a grey swirl, flat black) and the first applied. | The rings are pseudo-elements at negative z-index inside each card's own stacking context, so they never sit over content; cards that already used a pseudo-element for the sweep are excluded from the corner ring. |
| D40 | 2026-09-11 | The alternate section grounds carry a generated fluid swirl in black, charcoal and silver (Nano Banana 2, 16:9 at 2K, delivered at 1920 px WebP, 47 KB), blurred 22 px at 85 percent opacity so it reads as a soft deformation of the black rather than a picture; the lifted tone mirrors it. The cards lose the corner ring of D39 and keep a plain hairline with a faint top-left light. | Owner, 11 September, from a glassmorphism reference: a form behind the glass so the blur shows, in black; then "the edges do not need that glow", "too sharp", "too subtle", settled on the middle strength (variant A of the third preview). Three swirls were generated (225 credits); one is used. | Light theme shows no swirl. The blur is a composited filter on one pseudo-element per section; the next Lighthouse run records any cost. |
| D41 | 2026-09-11 | The swirl is screened onto the same black as every other section, so only its pale ribbons remain, and those drift slowly (a 36 s ease, 2 percent of travel, a tenth of a degree of turn, alternating; the lifted tone runs half a cycle behind). Motion off stops the drift. | Owner, 11 September: the swirl's grey ground made the alternate sections lighter than their neighbours; the ribbons should move, mildly. | One composited transform per section; blur and blend are rasterised once. |
